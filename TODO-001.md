# Tasks

- [x] the line for the tabs in client page is pink color, make it light grey
    - [x] Locate the `TabBar` in `lib/screens/customer_detail_screen.dart`.
    - [x] Add `dividerColor: Colors.transparent` or set it to `AppTheme.lightGrey`.
    - [x] Ensure `indicatorColor` remains `Colors.black` as per theme.
- [x] now when the side bar opens it slides out from the right side, enhance the closing so that when it closes it slide out from left to right out of the screen, also make the close button a right arrow icon
    - [x] Refactor sidebar logic in `lib/screens/dashboard_screen.dart` and `lib/screens/customer_detail_screen.dart`.
    - [x] Use `AnimatedPositioned` for the sidebar.
    - [x] Change `Icons.close` to `Icons.chevron_right` for the close button.
    - [x] Update the `right` property logic to ensure smooth sliding out to the right.
- [x] during ai onboarding conversation, use the flutter genui sdk to let the ai display filled or partially filled client information during the chat to better inform the user what the end result will be and the current progress
    - [x] Review `flutter_ai_toolkit` documentation for custom response builders or GenUI integration.
    - [x] Modify `lib/widgets/chat_view.dart` to support a custom `responseBuilder`.
    - [x] Implement a `ClientInfoCard` widget that displays `Customer` fields.
    - [x] Update `AiService` to include structured data in the AI responses that the `responseBuilder` can parse.
- [x] integrate flutter feedback package for sending feedback, add a feedback button next to the setting button on home screen and a feedback button to the right end of the app bar in client screen to trigger feedback
    - [x] Add `feedback: ^3.1.0` to `pubspec.yaml`.
    - [x] Wrap `MaterialApp` with the `BetterFeedback` widget in `lib/main.dart`.
    - [x] Add the feedback `IconButton` in `dashboard_screen.dart` next to settings.
    - [x] Add the feedback `IconButton` in `customer_detail_screen.dart` app bar actions.
    - [x] Implement the `BetterFeedback.of(context).show(...)` logic to handle feedback submission.
