# Tasks

## 1. Persistent Local Mode ("No Sign In")
Implement a fully functional, offline-first experience that doesn't require Firebase Auth.

- [x] **Infrastructure: Add Local Database**
    - [x] Add `hive` and `hive_flutter` to `pubspec.yaml` for lightweight, reactive local storage.
    - [x] Initialize Hive in `main.dart`.
    - [x] Create Hive Adapters for `Cpa`, `Customer`, and `Engagement` models.

- [x] **Data Layer: Local Repositories**
    - [x] Implement `LocalCpaRepository`, `LocalCustomerRepository`, and `LocalEngagementRepository` using Hive.
    - [x] Ensure they follow the same interface/pattern as existing Firestore repositories.
    - [x] **Migration Logic**: Update `CpaProvider` to toggle between `FirebaseRepos` and `LocalRepos` based on the user's login state (e.g., `local_user` UID).

- [x] **UI: "Continue as Guest" Workflow**
    - [x] Update `LoginScreen` to replace "Demo Mode" with a "Continue as Guest" (Local Mode) option.
    - [x] In Local Mode, the app should save all data to the local database instead of returning static mock data.

## 2. Platform Branding & Asset Generation
Standardize the app's visual identity across all platforms using the raw design asset.

- [x] **Asset Generation (Web & Desktop)**
    - [x] Use `magick` (or `convert`) to generate `app/web/favicon.png` (32x32).
    - [x] Generate a high-res `app/assets/images/logo_icon.png` (1024x1024) for desktop icons.

- [x] **Mobile App Icons (Android & iOS)**
    - [x] Generate the necessary icon set for Android (`mipmap` densities).
        - [x] Square icons (48x48, 72x72, 96x96, 144x144, 192x192).
        - [x] Rounded icons.
    - [x] Generate the iOS AppIcon set (multiple sizes from 20x20 to 1024x1024).
    - [x] **Tooling**: Consider adding `flutter_launcher_icons` to `pubspec.yaml` to automate this process from a single high-res source generated by `magick`.

- [x] **Theme Integration**
    - [x] Ensure the generated icons use the `primaryIndigo` (#1A237E) background for a consistent "Premium Professional" look.
