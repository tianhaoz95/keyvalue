# Tasks

- [ ] add a feedback button to sign up screen
- [ ] the toggles in the settings sidebar turn pink when toggled off, make its color consistent with the app
- [ ] the feedback collection screen text field still activated to purple color, make it black
- [ ] for the date picker for start and end date in add schedule sidebar, do not pop up the date picker, just put the date picker in the sidebar as there is enough space
- [ ] in client page, the generate draft button only show up when there is no draft, but the user can have multiple draft, so it should always be there, also switch the feedback button and generate draft button location
- [ ] for the pop up draft deletion confirmation, make delete and cancel button on the same row
- [ ] make engagement schedules section collapsible, but expanded by default